#include<iostream>
using namespace std;
int main()
{
       int i;
       cout<<"enter any number";
       cin>>i;
       if(i>=90)
       {
          cout<<"1st class";
       }
          else if(i>=80)
          {
             cout<<"2nd class";
          }
               else if(i>=60)
               {
                 cout<<"3rd class";
               }
                  else if(i>=40)
                  {
                    cout<<"fail";
                  }
}

